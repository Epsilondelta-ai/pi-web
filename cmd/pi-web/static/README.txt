This directory is the Go embed target for the built Astro UI.

Run `bun run embed:assets` after `bun run build` to refresh these committed assets.
They are committed so release binaries include a complete UI.
